[phases.setup]
nixPkgs = ["chromium", "chromedriver", "python311", "python311Packages.pip", "libpq"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "python3.11 algarve_monitor.py"
